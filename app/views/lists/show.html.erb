<h1>List information</h1>

<%= link_to "Add user to share", list_user_search_path(@list), method: :get %>
<br>
<br>

<%= render partial: "share_users/shared_users"%>

<%= render "form"%>

<%= button_to "Cancle", user_path(current_user.id), method: :get %>
<%= button_to "Delete", list_path(@list.id), method: :delete %>